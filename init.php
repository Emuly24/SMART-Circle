<?php

declare(strict_types=1);

/**
 * Global application initializer.
 *
 * Include once at the top of any script that needs autoloading or environment config:
 *
 *   require_once __DIR__ . '/init.php';
 *
 * For full app config (session, helpers), use config.php instead — it loads this file.
 */

require_once __DIR__ . '/config/bootstrap.php';
